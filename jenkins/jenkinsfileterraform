pipeline {
    agent ubuntu_agent

    stages {
     
        stage('Terraform init') {
            steps {
                sh 'cd terraform'
                sh 'terraform init'
            }
        }
  
        
    }
}
