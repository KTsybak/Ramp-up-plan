pipeline {
    agent { label 'ubuntu_agent' }

    stages {
     
        stage('Terraform init') {
            steps {
                sh 'cd terraform'
                sh '/home/jenkins/workspace/terraform/terraform'
            }
        }
  
        stage('Terraform apply') {
            steps {
                sh 'terraform apply --auto-approve'
            }
        }
    }
}
